<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BusinessPlus" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/khand_semi_bold</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/main_bg</item>
        <item name="android:navigationBarColor">@color/nav_bar_color</item>

    </style>

    <style name="Theme.BusinessPlus" parent="Base.Theme.BusinessPlus" />

    <style name="et_focus_unfocus">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">@drawable/et_bg</item>
        <item name="android:fontFamily">@font/khand_semi_bold</item>
        <item name="android:padding">12dp</item>
        <item name="android:singleLine">true</item>
    </style>
</resources>